<!DOCTYPE html>
<html>
  <head>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.1/css/bootstrap.min.css" integrity="sha384-WskhaSGFgHYWDcbwN70/dfYBj47jz9qbsMId/iRN3ewGhXQFZCSftd1LZCfmhktB" crossorigin="anonymous">
   
    <style>
    ul {
    list-style-type: none;
    margin: 0;
    padding: 0;
    overflow: hidden;
    background-color: #333;
}

li {
    float: left;
}

li a {
    display: block;
    color: white;
    text-align: center;
    padding: 14px 16px;
    text-decoration: none;
}

li a:hover:not(.active) {
    background-color: #111;
}

.active {
    background-color: #4CAF50;
}
      {
        background-color: #fff;        
      }
      html{
          color: #636b6f;
          font-family: 'Raleway', sans-serif;
          font-weight: 100;
          height: 90vh;
          margin: 0;
      }
      body{
        background-color: #cccccc;
      }
      .full-height {
          height: 100vh;
      }
      .flex-center {
          align-items: center;
          display: flex;
          justify-content: center;
      }
      .position-ref {
          position: relative;
      }
      .top-right {
          position: absolute;
          right: 10px;
          top: 18px;
      }
      .content {
          text-align: center;
      }
      .title {
          font-size: 84px;
      }
      .links > a {
          color: #636b6f;
          padding: 0 25px;
          font-size: 12px;
          font-weight: 600;
          letter-spacing: .1rem;
          text-decoration: none;
          text-transform: uppercase;
      }
      .m-b-md {
          margin-bottom: 30px;
      }
      .smaller {
        font-size: 16px;
      }


  </style>
  </head>
  <body>
  <ul>
  <li><a href="/">Home</a></li>
  <li><a href="/MyRecipes">My Personal Recipes</a></li>
  <li><a href="/My Ingredients">My Ingredients</a></li>
  <li style="float:right"><a class="active" href="/suggestions">Suggestions</a></li>
</ul>

#This page is going to show all possible recipes with check boxes next to each for the user to pick which
#They want to recipe they want to see on their home page. 

#params all ingredients and all recipes 

@allingredients.each do |ingredient|
    @allrecipes.each do |recipe|

if ingredient.name==recipe.name
    <input id="checkBox" type="checkbox">
    <% ingredient.name %>

    <% end %> 

